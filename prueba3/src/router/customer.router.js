const express = require('express');

const router = express.Router();


//routing
router.get('/',
    async (req, res, next) => {
        try {
            
        } catch (error) {
            next(error);
        }
    }
);